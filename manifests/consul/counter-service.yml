apiVersion: v1
kind: Service
metadata:
  name: counter-service
  labels:
    app: counter
    version: consul
spec:
  selector:
    app: counter
    version: consul
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8081
